{
  "version": 2,
  "builds": [
    { "src": "api/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.py" }
  ],
  "installCommand": "pip install torch==2.8.0+cpu -f https://download.pytorch.org/whl/cpu && pip install -r requirements.txt"
}
